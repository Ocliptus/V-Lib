<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.r00t.v_lib">

    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activities.start.StartAct">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MVPTestAct" />
        <activity android:name=".activities.sign.in.SignInAct" />
        <activity android:name=".activities.sign.up.SignUpAct" />
        <activity android:name=".activities.main.MainAct" />
        <activity android:name=".activities.addBook.addBookAct" />
        <activity android:name=".activities.addBook.manuel.addByManuelAct" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".activities.addBook.isbn.isbnAct" />
        <activity android:name=".activities.myLibrary.myLibraryAct"></activity>
    </application>

</manifest>